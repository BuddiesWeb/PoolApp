generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model player {
  id   Int    @id(map: "player_pk") @unique(map: "player_id_uindex") @default(autoincrement())
  name String @db.VarChar(50)
}
